# A post hook gets executed after a subcommand is run.
# It must be executable by the user (keep in mind that you might need a
# shebang).  Each subcommand behaves slightly differently around hooks, so you
# are advised to consult the manpage beforehand.



# vim: ft=sh
